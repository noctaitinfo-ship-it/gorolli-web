# REDESIGN NOTES — "The Finder" (demand-first homepage)

Branch: `redesign/gorolli-finder-demand-first` — create it yourself:

```bash
git checkout main
git checkout -b redesign/gorolli-finder-demand-first
# copy the contents of this folder over the repo root (index.html + the 7 language pages)
git add index.html en.html et.html de.html fi.html lv.html lt.html pl.html REDESIGN-NOTES.md
git commit -m "Finder demand-first homepage — face redesign, all integrations preserved"
```

Do **not** deploy, do **not** push to main. The previous redesign branch stays untouched as backup.
`robots.txt`, `sitemap.xml`, `privacy.html`, `/screenshots`, `/video` and both mp4 files are **unchanged** — nothing in this folder replaces them. (The mp4s are not duplicated here; the new pages reference them at their existing repo paths.)

---

## 1 · Integration preservation map

### A. Language system — PRESERVED VERBATIM (index.html, bottom scripts)
| Block | Status |
|---|---|
| `SUPPORTED` 32-language list | verbatim |
| `C2L` country→language map | verbatim |
| `grHi` pill highlight | verbatim (pills restyled via CSS only; classnames `.lpill`/`.on`, `data-l`, `#langbar` ids unchanged) |
| `grTranslateCode` he→iw, zh→zh-CN | verbatim |
| `grCombo` combo driver + 80×200ms retry | verbatim |
| `grRestore` Estonian restore (googtrans cookie deletion, no reload) | verbatim |
| `grAppLinks` `?lang=` rewriting | verbatim |
| `grSet`, langbar click handlers + `localStorage['gr_lang']` | verbatim |
| Auto-detect order: saved → ipapi country → browser language → `en` | verbatim |
| Anti-banner 300ms watchdog | verbatim |
| GT widget init (`pageLanguage:'et'`, `autoDisplay:false`) + `element.js` | verbatim |
| GT chrome-hiding CSS (`.goog-te-*`, `body{top:0}`, `#google_translate_element`, `font[style]`) | verbatim, kept at end of new stylesheet |
| `notranslate`/`translate="no"` on wordmark + langbar | preserved |

**Marked modifications (2), both tagged `REDESIGN ADD` / commented:**
1. Inside the existing ipapi callback: 2 lines set the hero label "Sinu lähedal — {city}" from the *same* response. Wrapped in try/catch, no new requests, fails silently. Note: users with a saved language skip ipapi (existing behavior), so they see the fallback "Sinu lähedal — Eesti".
2. `openVideo(card)` now reads `data-src` (two videos instead of one). Still click-to-play. It no longer adds `loop muted` after the click, so sound plays — add those attributes back if you prefer the old behavior.

**Removed with their DOM:** the screenshot slideshow script (`.scr`/`.slide` rotation) — purely presentational, its markup is gone.

### B. Country/location — PRESERVED + EXTENDED
- ipapi.co fetch, order and fallbacks untouched; Finder city label piggybacks on it (see A.1).
- The live-map geolocation flow (below) runs **only** behind `live_map=true` and never replaces the language detection.

### C. `?lang=` web-app links — PRESERVED
- `grAppLinks` untouched. Every new client/host link starts with `https://client.gorolli.com` / `https://host.gorolli.com`, so the existing `href^=` selector rewrites **all** of them on every language change (hero CTA, header host pill, host section, app cards, web buttons).
- Static language pages hardcode `?lang=xx` matching their own route.

### D. App links — EXACT, unchanged
- Client web `https://client.gorolli.com` · Host web `https://host.gorolli.com`
- Client Play `…id=com.mycompany.Hotelio` · Host Play `…id=com.mycompany.gorollihostapp`
- App Store (temporary search) `https://apps.apple.com/us/search?term=GoRolli`
- Client links live only in the client card/CTA; host links only in host contexts — never mixed. Store badge markup (incl. `title` attributes) reused verbatim from the old page.

### E. SEO / routes — HEADS BYTE-EXACT
- `index.html` head (title, description, theme-color, canonical, full hreflang set, OG/Twitter, JSON-LD, robots meta, favicon + inline SVG icon) is copied unchanged.
- The 7 language pages were **generated by extracting each original `<head>` byte-for-byte** (everything before `<style>`), plus their original `kw` keyword line, `langnav` (with correct `.on`) and localized `<footer>` verbatim. Only styles + body content are new.
- `/en /et /de /fi /lv /lt /pl` filenames unchanged; robots.txt + sitemap.xml untouched; `/privacy` link kept.

---

## 2 · Content changes to sign off
- **Removed "1000+ haagist"** — unverifiable inventory claim (per your rules).
- **Removed "kuni 50€ nädalas"** — earnings promise (per your rules). Host copy is mechanics-only.
- **Removed the 50€ lottery promo card** — not in the approved section list. If the campaign is still live, say so and I'll re-add it as a small card in the "Vaata lähemalt" section.
- **Removed the autoplay video hero** and the equal Client/Host device stage. Marketing video is now click-to-play below the fold; apps live in their own section below the fold.
- No web fonts: system font stack everywhere (deliberate — zero blocking third-party CSS, instant first paint).

## 3 · Feature flag + backend contract (`live_map`)
`index.html` defines `var GOROLLI_FLAGS = { live_map: false }`.

- **false (today):** map is a labeled illustrative preview ("Illustratiivne eelvaade"); the CTA is a plain `<a>` to `client.gorolli.com?lang=xx` — works with JS disabled. No geolocation prompt ever fires.
- **true (when API exists):** tap → geolocation ask → granted: `GET /api/home/nearby?lat&lng&r=5000` → shows count / from-price / nearest-km + "Ava täisotsing"; count 0 → wider-area re-query (25 km) + honest host invite; denied → manual city input (routes to client web). API failure → straight to client web, never a dead page.

Backend must provide (anonymous, cacheable ~60s at the edge):
```
GET /api/home/nearby?lat=59.437&lng=24.745&r=5000
→ { count, min_price:{amount,currency}, nearest_km,
    pins:[{lat,lng,type,price_from}] }   // coords rounded to ~300m grid server-side,
                                         // max ~30 pins, no IDs/addresses/host names
```
Optional later: per-area search count (last 30 d) to power "renters are already looking" honestly — the line is **not** shown until that data exists. The `&q=` city parameter passed by manual search needs a matching param on client.gorolli.com (TODO: confirm the real query param name).

## 4 · Performance
- First paint = static HTML + one inline stylesheet; hero needs **zero JS**; CTA is a real link (JS-off fallback → client.gorolli.com).
- Map preview = inline SVG (~1 KB). No map library exists on the page today; with `live_map=true` still no tiles — only one fetch on tap.
- Only third-party requests: ipapi.co + Google Translate (both preserved behavior), and they load after content.
- Videos: poster images only; `<video>` injected on click; no autoplay anywhere.
- Removed: 4.3 MB autoplay hero video from first paint, 11 screenshot images, slideshow timers.

## 5 · Test checklist (run on the branch before merging)
- [ ] Mobile 390px: first screen complete (logo, headline, location card, CTA, no-app line, host line), no horizontal scroll
- [ ] Language switching: every pill translates in place, no reload loop, Google banner stays hidden
- [ ] EE pill → restores Estonian (cookie cleared, no reload)
- [ ] he → iw and zh → zh-CN still map correctly
- [ ] Saved choice persists (`localStorage.gr_lang`) across reloads
- [ ] Country detection: fresh visitor from FI/DE/EE/LV/LT/PL gets that language (ipapi), VPN test
- [ ] `?lang=` — after switching language, hero CTA + all client/host links carry the new `lang`
- [ ] Client links: Play `com.mycompany.Hotelio`, App Store search, client.gorolli.com — correct and never mixed with host
- [ ] Host links: Play `com.mycompany.gorollihostapp`, App Store search, host.gorolli.com
- [ ] /en /et /de /fi /lv /lt /pl load, hreflang/canonical/OG/JSON-LD identical to old pages (diff the heads)
- [ ] GPS allowed (set `live_map:true` + mock API): count state renders
- [ ] GPS denied: manual city input appears, routes to client web
- [ ] No trailers nearby (mock `count:0`): wider-area + host invite state
- [ ] `live_map:false`: no geolocation prompt, CTA navigates directly, map labeled as preview
- [ ] Videos play only on click; no autoplay on load
- [ ] JS disabled: page renders, CTA and all store/web links work

## 6 · Files in this folder
`index.html` (root, ET + GT), `en/et/de/fi/lv/lt/pl.html` (static, zero-JS), `REDESIGN-NOTES.md`, plus copies of `favicon.png` and the two poster images **only so the preview here works** — they're identical to the repo's own files; don't overwrite anything binary when copying to the repo.
